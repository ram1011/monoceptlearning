import { CreditcardnumberformaterPipe } from './creditcardnumberformater.pipe';

describe('CreditcardnumberformaterPipe', () => {
  it('create an instance', () => {
    const pipe = new CreditcardnumberformaterPipe();
    expect(pipe).toBeTruthy();
  });
});
